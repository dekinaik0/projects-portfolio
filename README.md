# Портфолио: Python, SQL, аналитика данных

Три пет-проекта по работе с данными: от SQL-аналитики до автоматизации отчётности и текстовой классификации. Каждый проект лежит в отдельной папке, имеет свой README и запускается независимо.

## Проекты

| Проект | О чём | Стек |
|---|---|---|
| [01 — SQL-аналитика интернет-магазина](01-sql-shop-analytics) | Схема БД, 10 запросов от агрегаций до оконных функций, продуктовые метрики и дашборд | SQLite, pandas, matplotlib |
| [02 — Классификация отзывов](02-review-classifier) | Размеченный датасет, TF-IDF + LinearSVC, кросс-валидация, precision/recall/F1 и разбор ошибок | scikit-learn, pandas, matplotlib |
| [03 — Автоматизация отчёта по заявкам](03-report-automation) | Очистка «грязной» выгрузки, расчёт SLA и метрик поддержки, сборка Excel-отчёта с графиками | pandas, openpyxl |

## Что демонстрируют проекты

**SQL** — JOIN, CTE, оконные функции (`RANK`, `SUM OVER`), агрегации, группировка по датам, расчёт retention и среднего чека.

**Python и работа с данными** — очистка сырых данных, разбор неоднородных форматов, агрегации и разрезы в pandas, выгрузка в Excel с диаграммами.

**ML** — векторизация текста, линейные классификаторы, выбор схемы валидации под размер выборки, интерпретация метрик по классам.

**Оценка качества** — не только «модель работает», но и где именно она ошибается, какие классы путает и что из этого стоит отдавать на ручную проверку.

## Данные

Во всех проектах данные синтетические и генерируются скриптами с фиксированным `random.seed`. Это сделано намеренно: набор воспроизводится у любого, кто клонирует репозиторий, и в него можно заложить ровно те дефекты, на которых интересно показать логику обработки — дубли, разные форматы дат, пропуски, разнобой в регистре.

## Как запускать

Каждая папка самодостаточна:

```bash
cd 01-sql-shop-analytics
pip install -r requirements.txt
python create_db.py
python metrics.py
```

Точные команды для остальных проектов — в их README.

## Структура

```
projects-portfolio/
├── README.md
├── .gitignore
├── 01-sql-shop-analytics/
│   ├── README.md
│   ├── requirements.txt
│   ├── create_db.py
│   ├── queries.sql
│   ├── metrics.py
│   └── dashboard.png
├── 02-review-classifier/
│   ├── README.md
│   ├── requirements.txt
│   ├── reviews.csv
│   ├── train.py
│   └── evaluate.py
└── 03-report-automation/
    ├── README.md
    ├── requirements.txt
    ├── make_raw_data.py
    ├── raw_tickets.csv
    └── build_report.py
```

Нумерация папок задаёт порядок отображения на GitHub — сверху лежит то, что стоит смотреть первым.
